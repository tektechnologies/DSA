'use strict';

const linkedList = require('../linked_list');

describe('arrayShift', () => {
  it('', () => {
    expect().toEqual();
  });

  it('', () => {
    expect().toEqual();
  });

  it('', () => {
    expect().toEqual();
  });

  

  it('', () => {
    expect().toBe();
  });

  it('returns given array if value is missing', () => {
       
    expect().toBe();
  });
});