'use strict';

const linkedList = require('../linked_list');

describe('linkedList', () => {
  it('', () => {
    expect().toEqual();
  });



  it('returns given array if value is missing', () => {
       
    expect().toBe();
  });
});

describe('includes', () => {
  it('', () => {
    expect().toEqual();
  });

  it('', () => {
    expect().toEqual();
  });

  
});