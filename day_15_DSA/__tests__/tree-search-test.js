'use strict';



const TreeSearch = require('../tree-search');

describe('sort search trnpm ee', () => {
  it('has null data for root', () => {
    let sTree = new TreeSearch();
    expect(sTree.root).toBe(null);
  });

});